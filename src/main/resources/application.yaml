spring:
  application:
    name: coupon
  profiles:
    active: local
  session:
    store-type: redis
    timeout: 30m
    redis:
      namespace: coupon:session
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        default_batch_fetch_size: 100
