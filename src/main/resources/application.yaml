spring:
  application:
    name: coupon
  profiles:
    active: local
  session:
    store-type: redis
    timeout: 30m
    redis:
      namespace: coupon:session
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        default_batch_fetch_size: 100
